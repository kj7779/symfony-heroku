<form method="post" action="{{ path('app_diary_delete', {'id': diary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diary.id) }}">
    <button class="btn">Delete</button>
</form>
